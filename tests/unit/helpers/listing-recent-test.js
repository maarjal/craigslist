import { listingRecent } from 'craigslist/helpers/listing-recent';
import { module, test } from 'qunit';

module('Unit | Helper | listing recent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingRecent([42]);
  assert.ok(result);
});
